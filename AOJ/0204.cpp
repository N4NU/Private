#include <iostream>
#include <math.h>
#include <vector>
#include<algorithm>
using namespace std;

struct Point{
	double distance;
	int v;
};

int main()
{
	Point UFO_Point[100];
	do{
		cin >> R >> N;
		if 
		cin >> x0 >>y0 >> r >> v:
		UFO_Point[i].distance=sqrt(x0*x0+y0*y0)-R+r;
	}while (R==0&&N==0);
}